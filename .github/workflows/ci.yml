name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-typescript:
    name: Build TypeScript
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: |
            packages/moe-daemon/package-lock.json
            packages/moe-proxy/package-lock.json

      - name: Install and build daemon
        run: |
          npm ci
          npm run build
        working-directory: packages/moe-daemon

      - name: Install and build proxy
        run: |
          npm ci
          npm run build
        working-directory: packages/moe-proxy

      - name: Upload daemon build
        uses: actions/upload-artifact@v6
        with:
          name: daemon-dist
          path: packages/moe-daemon/dist/

      - name: Upload proxy build
        uses: actions/upload-artifact@v6
        with:
          name: proxy-dist
          path: packages/moe-proxy/dist/

  test-typescript:
    name: Test TypeScript
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: |
            packages/moe-daemon/package-lock.json
            packages/moe-proxy/package-lock.json

      - name: Install daemon dependencies
        run: npm ci
        working-directory: packages/moe-daemon

      - name: Install daemon coverage provider
        run: npm install --no-save --no-package-lock @vitest/coverage-v8@^1.6.0
        working-directory: packages/moe-daemon

      - name: Test daemon with coverage
        run: npm run test:coverage
        working-directory: packages/moe-daemon

      - name: Upload daemon coverage
        uses: actions/upload-artifact@v6
        with:
          name: daemon-coverage
          path: packages/moe-daemon/coverage/

      - name: Install proxy dependencies
        run: npm ci
        working-directory: packages/moe-proxy

      - name: Install proxy coverage provider
        run: npm install --no-save --no-package-lock @vitest/coverage-v8@^1.6.0
        working-directory: packages/moe-proxy

      - name: Test proxy with coverage
        run: npm run test:coverage
        working-directory: packages/moe-proxy

      - name: Upload proxy coverage
        uses: actions/upload-artifact@v6
        with:
          name: proxy-coverage
          path: packages/moe-proxy/coverage/

  build-plugin:
    name: Build JetBrains Plugin
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: packages/moe-daemon/package-lock.json

      - name: Build daemon
        run: |
          npm ci
          npm run build
        working-directory: packages/moe-daemon

      - name: Setup JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v5

      - name: Build plugin
        run: |
          chmod +x ./gradlew
          ./gradlew buildPlugin
        working-directory: moe-jetbrains

      - name: Run plugin tests
        run: ./gradlew test
        working-directory: moe-jetbrains

      - name: Upload plugin zip
        uses: actions/upload-artifact@v6
        with:
          name: jetbrains-plugin
          path: moe-jetbrains/build/distributions/*.zip

  # Cross-platform build verification
  build-all-platforms:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Build daemon
        run: |
          npm ci
          npm run build
        working-directory: packages/moe-daemon

      - name: Build proxy
        run: |
          npm ci
          npm run build
        working-directory: packages/moe-proxy
