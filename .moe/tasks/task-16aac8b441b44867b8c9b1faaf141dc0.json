{
  "id": "task-16aac8b441b44867b8c9b1faaf141dc0",
  "epicId": "epic-be924fb8",
  "title": "docs: \"the daemon never runs git\" is false in pre-Wave-1 text; it runs a read-only git status for handoff fingerprints",
  "description": "Found by the Wave 1 gate (task-808869c5, round 2). QA rejected that card because docs/ARCHITECTURE.md said the daemon \"never runs git\". The daemon runs exactly one git command: a read-only `git --no-optional-locks status --porcelain=v2 --branch` in packages/moe-daemon/src/util/diskState.ts:43-57, which fingerprints the working tree for handoff notes. claim_next_task (tools/claimNextTask.ts:794) and release_task (tools/releaseTask.ts:166) call it. task-808869c5 fixed ARCHITECTURE.md and the Wave 1 record sections of SCHEMA.md, MCP_SERVER.md and CONFIGURATION.md. Seven older statements still say the daemon never runs git:\n\n- CLAUDE.md:86 (Land on every exit: \"the daemon is state-only and never runs git\")\n- docs/SCHEMA.md:108 (autoCommit settings comment)\n- docs/SCHEMA.md:448 (commit ledger comment)\n- docs/CONFIGURATION.md:156 (autoCommit row)\n- docs/MCP_SERVER.md:261 (get_context Commit evidence)\n- docs/MCP_SERVER.md:450 (complete_task verification: \"never executes the command itself and never runs git\")\n- docs/MCP_SERVER.md:568 (get_commit_scope: \"State-only — the daemon never runs git\")\n\nFix: reword each to state what holds, matching docs/ARCHITECTURE.md \"Delivery Path\". The daemon never writes git state, lands, pushes or runs the gate, and it never consults git to verify what a runner reported; its one git call is the read-only status fingerprint. Docs only, no code. CLAUDE.md is also declared by task-31fe6dc8, so land after it or coordinate. The epic rail for epic-3d096d59 uses the same phrase; leave that to its owner.",
  "definitionOfDone": [
    "Each of the seven listed statements states what holds: no git writes, landing, push, or gate run by the daemon, and no git check of runner-reported values; the read-only status fingerprint is named or not contradicted",
    "cd packages/moe-daemon && npx vitest run src/util/taskTimeBudgetDocs.test.ts passes, and npm run lint passes at the repo root"
  ],
  "taskRails": [],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "RED FIRST: a doc-contract guard, in the style of src/util/taskTimeBudgetDocs.test.ts (it reads the documents off disk and asserts they match the daemon's behaviour). This task waits on task-31fe6dc8 (CLAUDE.md), task-dad7eda4 (SCHEMA.md, MCP_SERVER.md) and task-d5b7e6a2 (CONFIGURATION.md, MCP_SERVER.md), so every file is free when you start; line numbers below are from bb5e7e2 and will have moved, so find each sentence by its text.\n\nNew file packages/moe-daemon/src/util/daemonGitDocs.test.ts, with repoRoot resolved exactly as taskTimeBudgetDocs.test.ts does (src/util -> src -> moe-daemon -> packages -> root). Two it() blocks:\n1. 'no doc says the daemon never runs git'. Scan CLAUDE.md plus every *.md under docs/ (recursive; today docs/plans, docs/roles and docs/skills are clean too, so no exclusions are needed) for /never (?:runs|executes|touches) git|runs no git|does not run git/i and collect 'relative/path:line' for each hit. Expect the list to equal []. Failing with the list, not a boolean, is what makes the red readable.\n2. 'the architecture doc names the one read-only git call, exactly as util/diskState.ts runs it'. Assert docs/ARCHITECTURE.md contains 'git --no-optional-locks status --porcelain=v2 --branch', and that src/util/diskState.ts contains \"['--no-optional-locks', 'status', '--porcelain=v2', '--branch']\". That ties the true statement to the code, so a later edit of either one fails here instead of drifting.\n\nRun: cd packages/moe-daemon && npx vitest run src/util/daemonGitDocs.test.ts. Test 1 must FAIL listing 8 sites: the task's 7 plus docs/DEVELOPMENT.md:184, which the task description missed. Test 2 passes today; it guards the statement the rewrites point at. Paste the red list into the step note.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "newFiles": [
        "packages/moe-daemon/src/util/daemonGitDocs.test.ts"
      ],
      "modifiedFiles": [
        "packages/moe-daemon/src/util/daemonGitDocs.test.ts"
      ],
      "note": "RED as planned. New packages/moe-daemon/src/util/daemonGitDocs.test.ts (LF, written with the Write tool). repoRoot is resolved the same way as in taskTimeBudgetDocs.test.ts. Two it() blocks:\n(1) Scan CLAUDE.md plus docs/**/*.md with fs.readdirSync(recursive). An arrayContaining guard names 6 top-level docs plus docs/roles/worker.md, so an empty or non-recursive walk fails instead of passing vacuously.\n(2) ARCHITECTURE.md contains the exact command, and diskState.ts contains the exact GIT_STATUS_ARGS array.\n\n`cd packages/moe-daemon && npx vitest run src/util/daemonGitDocs.test.ts` gives \"Tests 1 failed | 1 passed (2)\". Test 1 received:\n[\"CLAUDE.md:86\",\"docs/CONFIGURATION.md:156\",\"docs/DEVELOPMENT.md:184\",\"docs/MCP_SERVER.md:261\",\"docs/MCP_SERVER.md:450\",\"docs/MCP_SERVER.md:568\",\"docs/SCHEMA.md:108\",\"docs/SCHEMA.md:448\"]\nThat is the task's 7 sites plus DEVELOPMENT.md:184. Test 2 passes.\n\nVerified before writing:\n- diskState.ts:57 has GIT_STATUS_ARGS. It is the daemon's only git subprocess: grep for execFile/spawn('git') finds only diskState.ts:43, and index.ts spawn() runs node for the supervisor.\n- Callers are claimNextTask.ts:794 and releaseTask.ts:166.\n- tsconfig.json excludes src/**/*.test.ts, so a plain `tsc --noEmit` does not typecheck this file. Step 3 typechecks it explicitly.\n\nDependencies task-31fe6dc8, task-dad7eda4 and task-d5b7e6a2 have all landed (862a99f, 4141893, 1e6cd4e).\n\nDirty paths that are not mine were left untouched: docs/roles/*.reference.md, docs/skills/*, .moe/*, util/contextNextAction.ts, recommendSkill*, tools/startStep.ts and generated/skillFiles.ts.",
      "startedAt": "2026-09-19T07:30:27.447Z",
      "completedAt": "2026-09-19T07:32:09.977Z"
    },
    {
      "stepId": "step-2",
      "description": "REWORD THE 8 SITES. Say what holds, in the vocabulary of docs/ARCHITECTURE.md's 'Delivery Path': the daemon never writes git state, lands, pushes or runs the gate; it never checks runner-reported values against git; its one git call is a read-only `git --no-optional-locks status --porcelain=v2 --branch` fingerprint for handoff notes (util/diskState.ts, from claim_next_task and release_task). Keep each hunk to the sentence, because several of these files are long shared lines.\n\n1. CLAUDE.md, 'Land on every exit' bullet: '(... the wrappers are the only git actors; the daemon is state-only and never runs git)' becomes '(... the wrappers are the only git actors; the daemon is state-only: it never writes git state, lands, pushes or runs the gate, and its one git call is a read-only status fingerprint for handoff notes)'. This is the most-read copy, so it names the fingerprint.\n2. docs/SCHEMA.md, the autoCommit settings comment: '(the daemon never runs git)' becomes '(the daemon writes no git state and never lands or pushes)'.\n3. docs/SCHEMA.md, the commit-ledger comment: 'the daemon never runs git' becomes 'the daemon records what the wrapper reported and never checks it against git'.\n4. docs/CONFIGURATION.md, the autoCommit row: '(the wrappers are the only git actors; the daemon never runs git)' becomes '(the wrappers are the only git actors; the daemon writes no git state and never lands, pushes or runs the gate)'.\n5. docs/MCP_SERVER.md, get_context 'Commit evidence': '(the daemon never runs git)' becomes '(the daemon stores what the wrapper reported and never checks it against git)'.\n6. docs/MCP_SERVER.md, complete_task verification: 'The daemon never executes the command itself and never runs git.' becomes 'The daemon never executes the command itself, and never checks the report against git.'\n7. docs/MCP_SERVER.md, get_commit_scope: 'State-only - the daemon never runs git; the wrapper joins this with its own git status snapshot' becomes 'State-only - this answer reads no repository; the wrapper joins it with its own git status snapshot'. The rest of that sentence stays.\n8. docs/DEVELOPMENT.md, the post-flight paragraph: '(the daemon is state-only and never runs git)' becomes '(the daemon is state-only: it writes no git state and never lands, pushes or runs the gate)'. This is the site the task missed; the guard in step 1 cannot pass without it, which is why it is in scope.\n\nLeave the epic-3d096d59 rail text alone (the task says its owner decides), and leave code comments and tool descriptions alone: this task is docs only.",
      "status": "COMPLETED",
      "affectedFiles": [
        "CLAUDE.md",
        "docs/SCHEMA.md",
        "docs/CONFIGURATION.md",
        "docs/MCP_SERVER.md",
        "docs/DEVELOPMENT.md"
      ],
      "modifiedFiles": [
        "CLAUDE.md",
        "docs/SCHEMA.md",
        "docs/CONFIGURATION.md",
        "docs/MCP_SERVER.md",
        "docs/DEVELOPMENT.md"
      ],
      "note": "Reworded all 8 sites. Each hunk is a single line: git diff --stat shows 5 files, 8 insertions and 8 deletions. All edits used the built-in Edit tool, so they count as TOOL tier and keep LF: every file has 0 CR before and after.\n\nSix sites use the plan's wording as written: CLAUDE.md (it names the read-only status fingerprint), SCHEMA.md:108, CONFIGURATION.md:156, MCP_SERVER.md:261, MCP_SERVER.md:568 and DEVELOPMENT.md:184. MCP_SERVER.md:568 keeps its em dash.\n\nTwo sites deviate from the plan's wording on purpose, so that each rewrite is true in its context:\n- SCHEMA.md:448 now says \"the daemon stores what their callers report and never checks it against git\". The plan said \"what the wrapper reported\", but this ledger comment covers moe.record_commit (the wrapper) AND moe.declare_files (agents and governors), so \"the wrapper\" would be false for declaredFiles.\n- MCP_SERVER.md:450 now says \"The daemon never executes the command itself, and never checks these reports against git.\" The plan said \"the report\". The bullet covers three reported values: the verification evidence, the modifiedFiles seed and record_commit's landed paths. None of them is checked against git.\n\nTruth check:\n- The daemon's only git call is diskState.ts:43, from claimNextTask.ts:794 and releaseTask.ts:166.\n- getCommitScope.ts, recordCommit.ts, declareFiles.ts and completeTask.ts import no fs, child_process or git code. get_commit_scope's own description is \"Read-only apart from a liveness touch\".\n\nThe guard is now green: `cd packages/moe-daemon && npx vitest run src/util/daemonGitDocs.test.ts` gives \"Tests 2 passed (2)\". The epic-3d096d59 rail, code comments and tool descriptions were not touched.",
      "startedAt": "2026-09-19T07:32:13.746Z",
      "completedAt": "2026-09-19T07:33:32.887Z"
    },
    {
      "stepId": "step-3",
      "description": "VERIFY.\n1. cd packages/moe-daemon && npx vitest run src/util/daemonGitDocs.test.ts src/util/taskTimeBudgetDocs.test.ts: both green. The second one is the DoD's named doc test; it walks MCP_SERVER.md's tool headings, which these rewrites do not touch.\n2. cd packages/moe-daemon && npx tsc --noEmit: prints nothing (the new test is TypeScript).\n3. npm run lint at the repo root: passes. It is the role-doc linter, and no role doc changes, so this confirms nothing else moved.\n4. Paste the final scan: grep -rniE 'never (runs|executes|touches) git|runs no git|does not run git' CLAUDE.md docs/ prints nothing.\n5. For complete_task, verification is the step-1 vitest command with both files, with its tail as outputTail.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "VERIFY, all green. Runs are from the worktree on moe/work-2026-09-13.\n1. `cd packages/moe-daemon && npx vitest run src/util/daemonGitDocs.test.ts src/util/taskTimeBudgetDocs.test.ts` exits 0: Test Files 2 passed (2), Tests 19 passed (19).\n2. `cd packages/moe-daemon && npx tsc --noEmit` exits 0 with no output. tsconfig.json excludes src/**/*.test.ts, so that run does not cover the new test. I also typechecked it on its own: `npx tsc --noEmit --ignoreConfig --strict --target ES2022 --module ESNext --moduleResolution bundler --esModuleInterop --skipLibCheck --types node src/util/daemonGitDocs.test.ts` exits 0. A probe file with a deliberate TS2322 fails under the same flags, so the flags do check.\n3. `npm run lint` at the repo root exits 0. architect.md is 38 lines, worker.md 25 and qa.md 25, all within the 40-line cap.\n4. The final scan `grep -rniE 'never (runs|executes|touches) git|runs no git|does not run git' CLAUDE.md docs/` prints nothing and exits 1 (no match).\n5. Regression check: the full daemon suite `npx vitest run` passes 123 of 123 test files and 2152 of 2152 tests, exit 0. No other test reads the edited docs: qaCommitPolicy.test.ts reads only docs/roles/qa.md and the moe-qa-loop SKILL.md.\nThis step changed no files.",
      "startedAt": "2026-09-19T07:33:38.973Z",
      "completedAt": "2026-09-19T07:34:30.477Z"
    },
    {
      "stepId": "step-4",
      "description": "SELF-REVIEW, posted as a task comment. Error handling does not apply beyond the test itself: it reads files with readFileSync, and a missing doc fails loudly rather than passing, which is the intended behaviour.\n1. Is each rewrite TRUE in its own context? The daemon writes no git state, never lands, pushes or runs the gate, never checks reported values against git, and runs exactly one read-only status call.\n2. Does any rewrite now contradict the fingerprint by implying the daemon never touches git at all?\n3. Does each hunk touch only its sentence? CLAUDE.md, SCHEMA.md and MCP_SERVER.md are long shared lines; show the git diff --word-diff of each.\n4. Is the guard neither too broad nor too narrow? Name one phrasing it would catch and one true sentence it must not catch ('the gate runs git hooks' does not match).\n5. List the code comments and MCP tool descriptions that still say 'never runs git' (for example in tools/recordDeliveryReceipt.ts, tools/finalizeAttempt.ts and delivery/policy.ts), so QA or the governor can decide on a follow-up. They were out of scope: docs only.\n6. git diff --stat: exactly the 6 planned paths.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "The self-review is posted as comment-f724b23151714d0da95045e4211785cb and covers all 6 plan items: truth in context, no contradiction of the fingerprint, word-diff per hunk, guard breadth with caught and not-caught examples, the out-of-scope list of code comments and tool descriptions, and the diff scope.\n\nThe comment also corrects my step-2 note. docs/SCHEMA.md and docs/DEVELOPMENT.md are CRLF in the worktree: that is git's autocrlf=true checkout form, and the Edit tool kept it. My plain `grep -c $'\\r'` count was a tool artifact. The committed blobs are LF, with only the planned lines changed: staging through a temp index gives numstat 1/1, 1/1, 1/1, 3/3, 2/2 and 82/0.\n\nThis step changed no files. All 6 task paths were reported in step-1 and step-2.",
      "startedAt": "2026-09-19T07:34:33.854Z",
      "completedAt": "2026-09-19T07:38:14.408Z"
    }
  ],
  "planRevision": 9,
  "status": "REVIEW",
  "assignedWorkerId": null,
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": null,
  "createdBy": "WORKER",
  "parentTaskId": null,
  "priority": "LOW",
  "order": 9,
  "comments": [
    {
      "id": "comment-f724b23151714d0da95045e4211785cb",
      "author": "worker-161b9959",
      "content": "SELF-REVIEW (step-4), worker-161b9959\n\n1. Each rewrite is TRUE in its own context. The daemon has one git subprocess, diskState.ts:43 (GIT_STATUS_ARGS at :57), called from claimNextTask.ts:794 and releaseTask.ts:166. index.ts spawn() runs node for the supervisor. getCommitScope.ts, recordCommit.ts, declareFiles.ts and completeTask.ts import no fs, child_process or git code.\n- SCHEMA.md:448 names \"their callers\": the ledger is written by record_commit (the wrapper) AND declare_files (agents and governors), so \"the wrapper\" would be false.\n- MCP_SERVER.md:450 says \"these reports\": that bullet covers three reported values, the verification evidence, the modifiedFiles seed and record_commit's landed paths.\n- MCP_SERVER.md:568 says \"this answer reads no repository\". It is scoped to get_commit_scope, whose own description reads \"Read-only apart from a liveness touch\".\n\n2. No rewrite implies the daemon never touches git. Each one negates writes, landing, push, the gate, or checking reports against git; none negates reading. CLAUDE.md names the read-only status fingerprint. \"The wrappers are the only git actors\" stays, matching ARCHITECTURE.md's \"The wrapper is the only git and process actor\" next to the fingerprint sentence.\n\n3. Each hunk touches only its sentence. git diff --word-diff:\n- CLAUDE.md: [-state-only and-]{+state-only: it+} {+writes git state, lands, pushes or+} [-git).-]{+the gate, and its one git call is a read-only status fingerprint for handoff notes).+}\n- CONFIGURATION.md: {+writes no git state and+} {+lands, pushes or+} [-git).-]{+the gate).+}\n- DEVELOPMENT.md: [-state-only-]{+state-only: it writes no git state+} {+lands, pushes or+} [-git).-]{+the gate).+}\n- MCP_SERVER.md: {+stores what the wrapper reported and+} [-runs-]{+checks it against+} | [-itself-]{+itself,+} [-runs-]{+checks these reports against+} | [-the daemon never runs git;-]{+this answer reads no repository;+} [-this-]{+it+}\n- SCHEMA.md: {+writes no git state and+} [-runs git).-]{+lands or pushes).+} | {+stores what their callers report and+} [-runs-]{+checks it against+}\n\n4. Guard breadth.\n- It catches \"the daemon never runs git\", \"never executes git\", \"never touches git\", \"runs no git\" and \"does not run git\", case-insensitive.\n- It must not catch, and does not catch, these true sentences: \"git hooks do **not** run unless settings.commitHooks=true\" (CLAUDE.md:86), \"hooks do **not** run\" (CONFIGURATION.md:160), \"The daemon never executes the command itself\" (MCP_SERVER.md:450, SCHEMA.md:1170), and \"the gate runs git hooks\".\n- Known ceiling, marked with a ponytail: comment in the test: the scan is per line, so a phrase wrapped across two lines slips through.\n- A wider probe (doesn't/never + run/invoke/call/spawn/use + git) finds nothing in docs today.\n- Vacuous-pass guard: arrayContaining over 6 top-level docs plus docs/roles/worker.md. An empty walk fails, and so does a non-recursive one (readdirSync recursive needs Node >=18.17, while engines says >=18.0).\n- A missing file throws, so the test fails loudly.\n- The scan reads the live docs/ tree, including untracked drafts in the shared checkout. docs/skills/ponytail* are clean today.\n\n5. OUT OF SCOPE (docs only), for a follow-up decision by QA or the governor. These code comments and MCP tool descriptions still say the daemon never runs git:\n- delivery/policy.ts:26\n- providers/providerDescriptor.ts:8\n- state/candidateStore.ts:19\n- state/receiptStore.ts:12\n- tools/finalizeAttempt.ts:26\n- tools/recordCandidate.ts:29, and its MCP description at :39\n- tools/recordDeliveryReceipt.ts:17, and its MCP description at :41\n- types/schema.ts:539, :625, :1024 and :1154\n- The epic-3d096d59 rail (.moe/epics/...json:9), left to its owner.\nThe receiptStore.test.ts:218 and :641 test names (\"never touches git or a process: no child process in the store/tool\") are true for those units.\n\n6. Diff scope is exactly the 6 planned paths. Staging them into a throwaway temp index, the same way the wrapper does (GIT_INDEX_FILE=... git add -- :(literal)<p>), gives this numstat against HEAD: CLAUDE.md 1/1, CONFIGURATION.md 1/1, DEVELOPMENT.md 1/1, MCP_SERVER.md 3/3, SCHEMA.md 2/2, and daemonGitDocs.test.ts 82/0 (new).\n\nCORRECTION to my step-2 note (\"every file has 0 CR before and after\"): that is wrong for two files. docs/SCHEMA.md and docs/DEVELOPMENT.md are CRLF in the worktree. That is git's autocrlf=true checkout form, which 48 other docs share, and the Edit tool kept it: birthtime equals mtime equals the edit time. My earlier count was a tool artifact: plain `grep -c $'\\r' f`, printed straight to the Bash tool's output, prints 0 on this box for an all-CRLF file, while `grep -Uc` and `$(grep -c ...)` print 1875. What lands is unaffected. The blobs git will commit (git hash-object, then cat-file, counted in node) have CR=0 and the same line counts as HEAD (105/1875/410/2766/439), and the numstat above shows no whole-file churn.\n\nVerification: daemonGitDocs + taskTimeBudgetDocs 19/19; tsc --noEmit clean, and the test file also typechecks alone under strict; npm run lint OK; the final docs scan is empty; full daemon suite 2152/2152 (123 files).",
      "timestamp": "2026-09-19T07:38:06.654Z"
    },
    {
      "id": "comment-6f0a6322abb34d33868feab2b50bdc94",
      "author": "worker-161b9959",
      "content": "CORRECTION to my completionSummary: its first lines say \"Nine doc lines\" and \"The ninth line was a duplicate on the same MCP_SERVER.md line (450)\". Both are wrong; please ignore that sentence. There were exactly 8 sites, the task's 7 plus DEVELOPMENT.md:184, one hit per line. They are the 8 listed under \"Sites:\" in the summary, which is complete and correct. The step-1 red run shows the same 8: CLAUDE.md:86, CONFIGURATION.md:156, DEVELOPMENT.md:184, MCP_SERVER.md:261/450/568 and SCHEMA.md:108/448. All 8 are reworded, and the guard is green.",
      "timestamp": "2026-09-19T07:38:44.561Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-18T21:40:47.793Z",
  "updatedAt": "2026-09-19T07:38:44.561Z",
  "metrics": {
    "firstClaimAt": "2026-09-18T21:43:29.507Z",
    "plannedStepCount": 4,
    "plannedDistinctFileCount": 6,
    "executedStepCount": 4
  },
  "contextFetchedBy": [
    "architect-e52c9adc",
    "worker-161b9959"
  ],
  "dependsOn": [
    "task-31fe6dc806d9466d9d71c9f8beda60be",
    "task-dad7eda464734535b1e7fb489f367793",
    "task-d5b7e6a21be5458c8b219243ed1a4db4"
  ],
  "planSizeWarnings": [
    "Plan touches 6 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-18T21:46:29.350Z",
  "planningNotes": {
    "approachesConsidered": "- Reword only the task's seven statements, with no test: rejected. It would break the epic-be924fb8 rail ('All fixes must include regression tests or test updates'), and this is the second pass over the same phrase: task-808869c5 fixed four other docs, and seven or eight more copies survived.\n- A guard that lists the exact files: rejected. A recursive scan of docs/ plus CLAUDE.md catches the next doc too, and it is satisfiable today, because plans, roles and skills are already clean.\n- Also sweep code comments and tool descriptions: rejected. The task is explicitly docs only; the self-review lists them for a follow-up decision.",
    "codebaseInsights": "- The daemon's only git call is util/diskState.ts: GIT_STATUS_ARGS = ['--no-optional-locks', 'status', '--porcelain=v2', '--branch'], run through execFile, from claim_next_task and release_task for handoff fingerprints.\n- docs/ARCHITECTURE.md's 'Delivery Path' already states the true invariant, fixed by task-808869c5; the rewrites reuse its vocabulary.\n- The full scan (CLAUDE.md plus docs/**/*.md) finds 8 sites: the task's 7 plus docs/DEVELOPMENT.md:184. A scan filtering on '.moe/' hid CLAUDE.md's line, because that line mentions .moe paths.\n- taskTimeBudgetDocs.test.ts is the repo's doc-contract test pattern: read docs off disk, assert they match behaviour, positive and negative halves.",
    "risks": "- Shared files: dependsOn task-31fe6dc8, task-dad7eda4 and task-d5b7e6a2, which edit CLAUDE.md, SCHEMA.md, CONFIGURATION.md and MCP_SERVER.md. Starting after them keeps each file uncontested at landing.\n- Keep the hunks minimal: CLAUDE.md's bullet is one very long line that peers edit often.\n- Do not reword the epic rail in .moe/epics; the task leaves it to its owner, and .moe is edited only through moe tools anyway.\n- The guard's regex must not catch true sentences. It anchors on the negated verb phrases only.",
    "keyFiles": [
      "CLAUDE.md",
      "docs/SCHEMA.md",
      "docs/CONFIGURATION.md",
      "docs/MCP_SERVER.md",
      "docs/DEVELOPMENT.md",
      "docs/ARCHITECTURE.md",
      "packages/moe-daemon/src/util/diskState.ts",
      "packages/moe-daemon/src/util/taskTimeBudgetDocs.test.ts"
    ]
  },
  "planApprovedAt": "2026-09-18T21:46:29.447Z",
  "workStartedAt": "2026-09-19T07:30:27.447Z",
  "reviewStartedAt": "2026-09-19T07:38:33.779Z",
  "verification": {
    "command": "cd packages/moe-daemon && npx vitest run src/util/daemonGitDocs.test.ts src/util/taskTimeBudgetDocs.test.ts",
    "exitCode": 0,
    "outputTail": " Test Files  2 passed (2)\n      Tests  19 passed (19)\n   Start at  10:38:18\n   Duration  766ms (transform 78%, import 16%, tests 4%, worker 1%)\n\n(npm run lint at repo root: exit 0; full daemon suite earlier this session: 123 files, 2152/2152 passed)",
    "reportedAt": "2026-09-19T07:38:33.779Z",
    "source": "agent-reported"
  },
  "filesModified": [
    "packages/moe-daemon/src/util/daemonGitDocs.test.ts",
    "CLAUDE.md",
    "docs/SCHEMA.md",
    "docs/CONFIGURATION.md",
    "docs/MCP_SERVER.md",
    "docs/DEVELOPMENT.md"
  ],
  "completionSummary": "Docs only, plus one new doc-contract guard test. Nine doc lines claimed the daemon \"never runs git\", which is false. Eight are reworded to say what holds, using ARCHITECTURE.md \"Delivery Path\" wording: the daemon never writes git state, lands, pushes or runs the gate, and never checks reported values against git. Its one git call is the read-only `git --no-optional-locks status --porcelain=v2 --branch` handoff fingerprint in util/diskState.ts, called from claim_next_task and release_task. The ninth line was a duplicate on the same MCP_SERVER.md line (450) and is covered by that rewrite.\n\nSites:\n- CLAUDE.md:86, which names the fingerprint\n- SCHEMA.md:108 and :448\n- CONFIGURATION.md:156\n- MCP_SERVER.md:261, :450 and :568\n- DEVELOPMENT.md:184, which the task description missed\n\nThe new packages/moe-daemon/src/util/daemonGitDocs.test.ts checks two things:\n(1) A recursive scan of CLAUDE.md and docs/**/*.md finds no /never (runs|executes|touches) git|runs no git|does not run git/i. An arrayContaining guard makes an empty or non-recursive walk fail.\n(2) ARCHITECTURE.md contains the exact command, and diskState.ts contains the same GIT_STATUS_ARGS array.\nRed-green: step 1 was red, listing all 8 sites; after the rewrites it is green.\n\nTwo wordings deviate from the plan so they are true in context. SCHEMA.md:448 says \"their callers\", because declare_files is agent-called. MCP_SERVER.md:450 says \"these reports\", because that bullet covers three reported values.\n\nVerification: the two doc tests pass 19/19. tsc is clean, and the test file also typechecks alone under strict. Lint passes. Full daemon suite 2152/2152.\n\nThe self-review is comment-f724b231. It lists the code comments and MCP tool descriptions that still say \"never runs git\", as a follow-up candidate: recordCandidate.ts:39 and recordDeliveryReceipt.ts:41 descriptions, plus comments in policy.ts, candidateStore.ts, receiptStore.ts, finalizeAttempt.ts, providerDescriptor.ts and schema.ts. The epic-3d096d59 rail is left "
}