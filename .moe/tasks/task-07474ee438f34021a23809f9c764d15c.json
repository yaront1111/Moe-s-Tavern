{
  "id": "task-07474ee438f34021a23809f9c764d15c",
  "epicId": "epic-3d096d59120542fc9fc9c7b91a713795",
  "title": "bug: every Wave 1 revision check refuses SHA-256 (64-hex) object ids, so a landing in a sha256 repository is refused and rescued",
  "description": "Found by QA of task-31fe6dc8 (comment-90d8b959, qa-7100be29; FYI from qa-5e66a826 in #general at 22:03Z). That card's DoD forbids behaviour changes, so it could not fix this. Every Wave 1 revision shape check assumes SHA-1: candidateStore SHA_RE accepts 7-40 hex and REVISION_RE exactly 40. In a repository created with --object-format=sha256 the object ids are 64 hex, so a `landed` finalize_attempt is refused 3 times and the wrapper parks the bytes on a rescue ref. This is rare, but it is untracked once task-31fe6dc8 closes. Pick ONE rule and apply it everywhere a revision is validated: either accept 64-hex ids, or refuse a sha256 repository up front at pre-flight with a message that names the object format.",
  "definitionOfDone": [
    "One documented rule for SHA-256 object-format repositories, applied in state/candidateStore.ts (SHA_RE, REVISION_RE) and in every caller that validates a revision",
    "A test using a 64-hex revision (or a sha256 fixture repository) fails on the old code and passes on the fix",
    "npx tsc --noEmit and the daemon suite stay green; both parity-check scripts pass if a wrapper changes"
  ],
  "taskRails": [],
  "implementationPlan": [
    {
      "stepId": "step-1",
      "description": "THE RULE, chosen from the two the task offers: Moe's delivery records hold SHA-1 object ids only. A repository whose object format is sha256 is refused up front and lands nothing, exactly like a no-git project.\n\nWhy refuse rather than accept 64-hex: accepting means changing the daemon regexes and every '7-40 hex' / '40 hex' message, tool description and doc line, plus the wrappers' zero-oid and empty-tree constants, their replay regexes and the harness fakes. That is a lot of surface for a rare format, against the epic's 'one narrow vertical slice' rail. Supporting sha256 later is a separate feature.\n\nRED FIRST, in BOTH harnesses (identical frozen-suite JS). This task waits on task-d1441364, task-84150869 and task-a5fb02e9, which edit these files, so find code by name.\n1. Add frozen mode 'sha256-repo'. Its fixture uses git(repo,'init','-q','--object-format=sha256') instead of the plain init; everything else stays as in 'pass'. Skip the mode, printing why, if `git init --object-format=sha256` is unsupported, which needs git older than 2.29 (CI and this box have newer).\n2. Assertions:\n   (a) the log contains 'MOE_COMMIT_REFUSED_OBJECT_FORMAT' and 'sha256';\n   (b) HEAD is unchanged and no refs/moe/rescue/* exist;\n   (c) no record_candidate, record_check_run or gate observation, since nothing is frozen or gated;\n   (d) exactly one finalize, outcome 'nothing-to-commit' (the documented no-git acknowledgement);\n   (e) owned.txt still holds the CLI's bytes, uncommitted.\n3. It must FAIL on today's wrappers. Their landing freezes a candidate with a 64-hex tree, the fake refuses it (it checks 40 hex, like the daemon), and the bytes are rescued. So (a), (b) and (d) are red. Run it on sh and pwsh and paste the failures.",
      "status": "COMPLETED",
      "affectedFiles": [
        "scripts/tests/postflight.sh",
        "scripts/tests/postflight.ps1"
      ],
      "modifiedFiles": [
        "scripts/tests/postflight.sh",
        "scripts/tests/postflight.ps1"
      ],
      "note": "RED arms added to the byte-identical frozen suite of BOTH harnesses with one node script (exact-once needles; the extracted suites compare equal, 61971 chars). Two arms:\n- sha256-repo: fixture made by `git init --object-format=sha256`, otherwise as 'pass'. One deepEqual pins all of (a)-(e) plus the print-once count: {refused: said('MOE_COMMIT_REFUSED_OBJECT_FORMAT')===1, named: log has 'uses the sha256 object format; Moe' (NOT bare 'sha256': the fixture path itself contains 'sha256-repo'), head unchanged, no refs/moe/rescue/*, candidates+checks+gate observations = 0, record_commit outcomes [], finals ['nothing-to-commit'], owned.txt = 'frozen owned\\n' while HEAD:owned.txt = 'base'}.\n- teardown-sha256 (beyond the plan's one arm, same machinery as teardown-no-git): the sh teardown_rescue checks `git -C \"$PROJECT\" rev-parse --show-toplevel` directly, not through git_top, so an interrupted sh session in a sha256 repo would acknowledge 'failed' instead of 'nothing-to-commit' even after the git_top fix. This arm pins that (expected outcome nothing-to-commit, no rescue ref, no record_commit).\n- Both skip with a printed reason when `git init --object-format=sha256` fails (git < 2.29). This box: git 2.54.\nRED, run in a scratch tree of HEAD scripts + only these arm edits (D:/moe-scratch/w0747-1KZuRv/old), MOE_FROZEN_TEST_ONLY=<arm>, MOE_POSTFLIGHT_TIMEOUT_SEC=400:\n- sh sha256-repo rc=1: actual {refused:0, named:false, rescue:'refs/moe/rescue/task-postflight/20260919T164210Z', evidence:1, commits:['committed','failed'], finals:['rescued']}; the log shows the fake refusing `assert.ok(sha(args.treeSha))` on a 64-hex tree and MOE_RESCUE_REF sha=67cd7d3a...6b0 (64 hex).\n- sh teardown-sha256 rc=1: actual 'rescued', expected 'nothing-to-commit'.\n- pwsh 7.6.6 sha256-repo rc=1: the same diff (rescue ref 20260919T164340Z, finals ['rescued']). pwsh teardown-sha256 rc=1: 'rescued' vs 'nothing-to-commit'.\nSHARED-CHECKOUT HAZARD: a Codex session (cwd D:\\projexts, started 16:21Z, prompt caching, its plan says leave uncommitted) is live-editing scripts/moe-agent.sh, moe-agent.ps1, postflight.sh and postflight.ps1, all PLANNED paths of this task. HEAD 469ed6d (a QA checkpoint) already swept its moe-agent.ps1 hunks in, so HEAD's ps1 needs the UNTRACKED scripts/prompt-cache*.mjs (MODULE_NOT_FOUND otherwise). I copied those helpers into the scratch tree ONLY so the HEAD ps1 wrapper could run. I flagged it in #general (msg-8f23b1a5, msg-a046584a) and by SendMessage to both interactive moes sessions (held for the human's approval). I have not reverted or committed any Codex bytes.",
      "startedAt": "2026-09-19T16:32:23.814Z",
      "completedAt": "2026-09-19T16:44:43.405Z"
    },
    {
      "stepId": "step-2",
      "description": "APPLY THE RULE AT THE ONE GIT PROBE in each wrapper: sh git_top() and ps1 Get-MoeGitTop, which every landing, baseline, marker, replay and rescue path calls first.\n\n1. After the top and gitdir are resolved, run `git -C <project> rev-parse --show-object-format`.\n2. If the answer is exactly 'sha256':\n   - print ONE line per wrapper process, under a flag so a loop does not repeat it: `[WARN] MOE_COMMIT_REFUSED_OBJECT_FORMAT: <top> uses the sha256 object format; Moe's delivery records hold SHA-1 ids only, so this seat lands nothing there, as in a no-git project.`;\n   - return the existing 'no repo' result (sh: return 1 with MOE_TOP etc. empty; ps1: $null).\n3. Any other answer, including an empty one from an old git that lacks the flag, proceeds exactly as today. A git that old cannot open a sha256 repository anyway.\n\nEverything downstream then takes its existing, tested no-git branch: no baseline, no candidate, no gate, no rescue ref, and the finalize acknowledges 'nothing-to-commit'. Confirm by reading that no landing path calls git without going through git_top or Get-MoeGitTop first. Name any exception in the step note, and route it through the probe if it would otherwise touch a ref.\n\nError handling: a failing rev-parse is treated as 'not sha256' (fail open to today's behaviour). The probe never throws, and under sh's set -e it stays inside an `|| fmt=\"\"` guard.\n\nMake the same change in both twins, with byte-identical operator text.",
      "status": "COMPLETED",
      "affectedFiles": [
        "scripts/moe-agent.sh",
        "scripts/moe-agent.ps1"
      ],
      "modifiedFiles": [
        "scripts/moe-agent.sh",
        "scripts/moe-agent.ps1"
      ],
      "note": "Rule applied at the one git probe of each twin, byte-identical operator line: `[WARN] MOE_COMMIT_REFUSED_OBJECT_FORMAT: <top> uses the sha256 object format; Moe's delivery records hold SHA-1 ids only, so this seat lands nothing there, as in a no-git project.`\n- sh git_top: after top+gitdir, `fmt=$(git -C \"$PROJECT\" rev-parse --show-object-format 2>/dev/null) || fmt=\"\"` (set -e safe); exactly 'sha256' -> print once under global MOE_OBJECT_FORMAT_WARNED, return 1 with MOE_TOP/REL/GITDIR empty. Every caller runs in the current shell (checked: no $(git_top)), so the flag holds across loop iterations; a hot reload (exec) starts over.\n- ps1 Get-MoeGitTop: same probe, `\"$format\".Trim() -ceq 'sha256'` -> print once under $script:MoeObjectFormatWarned, return $null. No Set-StrictMode in the wrapper, so the unset flag reads $null.\n- Old git: measured on git 2.54 that rev-parse ECHOES an unknown flag with rc=0 (`git rev-parse --show-object-formatx` prints `--show-object-formatx`), so a pre-2.29 git answers '--show-object-format', not 'sha256', and proceeds as today. The comments say that instead of \"empty\".\nAUDIT of git call sites:\n- sh: git_top callers are preflight_landing, receipt_replay (locals), the post-flight landing and no-landing branches, and the adoption count. Every other git call uses MOE_TOP/MOE_GITDIR (set only by git_top), $scratch (hook commit inside a landing) or $GATE_WORKSPACE (gate inside a landing).\n- ONE EXCEPTION: teardown_rescue ran `git -C \"$PROJECT\" rev-parse --show-toplevel` directly. It touches no ref, but in a sha256 repo it made an interrupted session acknowledge 'failed' instead of 'nothing-to-commit' (the ps1 twin already routes through Get-MoeGitTop). Routed through the probe as `[ -n \"${MOE_TOP:-}\" ] || (git_top) || return 0`. The subshell keeps MOE_TOP as the session left it, so every SHA-1 outcome is unchanged. Pinned by the teardown-sha256 arm.\n- ps1: Get-MoeGitTop callers are Invoke-MoeReceiptReplay, Invoke-MoeIdleRecovery, Invoke-MoeTeardownRescue, pre-flight, the post-flight autoCommit=false, no-landing and landing branches, and adoption. All other git calls take $Top/$Git.Top/$moeGit.Top from it, the hook $scratch, or the gate workspace.\nGREEN (scratch tree D:/moe-scratch/w0747-1KZuRv/new = HEAD scripts + arms + this fix): sha256-repo and teardown-sha256 each rc=0 'PASS frozen candidate scenarios: 1' on sh (Git Bash), pwsh 7.6.6 and PS 5.1.26100. Shared tree: both ps1 files parse with 0 errors on 7.6.6 and 5.1; bash -n ok; parity-check.sh and parity-check.ps1 PASS.\nShared-file note: I applied the same exact-once pieces to the shared wrappers ON TOP of the Codex session's uncommitted prompt-cache hunks: numstat went sh 37/25 -> 56/28 and ps1 32/24 -> 45/24, exactly my +19/-3 and +13. Codex hunks untouched, LF and BOM kept.",
      "startedAt": "2026-09-19T16:44:46.569Z",
      "completedAt": "2026-09-19T16:47:23.231Z"
    },
    {
      "stepId": "step-3",
      "description": "DOCUMENT THE ONE RULE where each reader meets it. No daemon behaviour changes: the regexes already refuse 64-hex ids, and that is now the stated contract, not an accident.\n1. packages/moe-daemon/src/state/candidateStore.ts, at SHA_RE and REVISION_RE: one comment saying the shapes are SHA-1 only BY RULE, and that the wrappers refuse a sha256 repository at the git probe (MOE_COMMIT_REFUSED_OBJECT_FORMAT), so no runner in this slice reports a 64-hex id. Point to docs/CONFIGURATION.md → autoCommit.\n2. docs/CONFIGURATION.md, the autoCommit row: one sentence. A repository created with --object-format=sha256 lands nothing (MOE_COMMIT_REFUSED_OBJECT_FORMAT, the same as a no-git project); commit such work by hand, and use deliveryPolicy manual-artifact if a strict policy applies.\n3. docs/SCHEMA.md, the Candidate 'Provenance' paragraph (the '7-40 hex' sentence): add 'SHA-1 object format only; see autoCommit for sha256 repositories'.",
      "status": "COMPLETED",
      "affectedFiles": [
        "packages/moe-daemon/src/state/candidateStore.ts",
        "docs/CONFIGURATION.md",
        "docs/SCHEMA.md"
      ],
      "modifiedFiles": [
        "packages/moe-daemon/src/state/candidateStore.ts",
        "docs/CONFIGURATION.md",
        "docs/SCHEMA.md"
      ],
      "note": "Rule documented where each reader meets it. No daemon behaviour change.\n1. candidateStore.ts: a 4-line comment above SHA_RE (covering SHA_RE and REVISION_RE). It says both are SHA-1 only BY RULE, a 64-hex id is refused, the wrappers refuse a sha256 repository at their git probe (MOE_COMMIT_REFUSED_OBJECT_FORMAT) so no runner in this slice reports one, and points to docs/CONFIGURATION.md autoCommit. SHA_RE is also used by checkRunStore, deliveryProjection and recordCommit, and REVISION_RE by receiptStore and finalizeAttempt. They all import these two constants, so the one comment covers every revision validator.\n2. CONFIGURATION.md autoCommit row: one sentence. A --object-format=sha256 repository lands nothing, exactly as a no-git project, with one `[WARN] MOE_COMMIT_REFUSED_OBJECT_FORMAT` line; commit by hand; under a strict deliveryPolicy follow its no-git rules (manual-artifact, with qualityGate unset or autoCommit false). DEVIATION from the plan's wording, for accuracy: the plan said just \"use manual-artifact\". The deliveryPolicy row already states that a no-git project with qualityGate set and autoCommit on still owes required-check under manual-artifact. That holds here too, because policy.ts cannot see the object format.\n3. SCHEMA.md Candidate Provenance: added \": SHA-1 object format only; see `autoCommit` in CONFIGURATION.md for sha256 repositories\" after the 7-40 hex sentence.\nEOLs kept (CONFIGURATION.md and candidateStore.ts LF, SCHEMA.md CRLF in the worktree). numstat: 1/1, 1/1, 4/0. Neither doc is vendored by the init or skill generators.\nChecks: `npx tsc --noEmit` rc=0. `npx vitest run src/state/candidateStore.test.ts src/util/daemonGitDocs.test.ts`: 2 files, 42/42 passed (the doc guard scans docs/**/*.md and src comments).",
      "startedAt": "2026-09-19T16:47:26.073Z",
      "completedAt": "2026-09-19T16:48:24.102Z"
    },
    {
      "stepId": "step-4",
      "description": "PARITY. The new MOE_COMMIT_REFUSED_OBJECT_FORMAT code is enforced by parity-check's MOE_COMMIT_* family diff automatically. Also add the prose needle 'uses the sha256 object format; Moe' to the attempt/gate prose lists of both parity-check.sh and parity-check.ps1, so the operator sentence cannot drift between twins. Prove the guard: misspell it in one wrapper, confirm both scripts fail, then restore it.",
      "status": "COMPLETED",
      "affectedFiles": [
        "scripts/tests/parity-check.sh",
        "scripts/tests/parity-check.ps1"
      ],
      "modifiedFiles": [
        "scripts/tests/parity-check.sh",
        "scripts/tests/parity-check.ps1"
      ],
      "note": "Needle 'uses the sha256 object format; Moe' appended to the attempt/gate prose list in parity-check.sh (the `for prose in ...` list) and in parity-check.ps1 ('attempt/gate prose' = @(...)). Each is a same-line edit through an exact-once node applier. parity-check.ps1 keeps its CRLF (139 CR / 139 LF before and after); numstat 1/1 each. MOE_COMMIT_REFUSED_OBJECT_FORMAT is covered by the MOE_COMMIT_* family diff automatically, since it is in both wrappers.\nGuard proof, run in scratch copies only (live ps1 seats hot-reload the repo wrapper):\n- Mutant `mut` = the fixed tree with the ps1 sentence misspelled ('object-format'; the code stays):\n  - parity-check.sh FAIL \"attempt/gate prose 'uses the sha256 object format; Moe' missing from: moe-agent.ps1\".\n  - parity-check.ps1 FAIL \"[attempt/gate prose] 'uses the sha256 object format; Moe' is missing from moe-agent.ps1\", on pwsh 7.6.6 and on PS 5.1 (rc=1 each).\n- Control `ctl` = the same mutant wrapper with the parity scripts WITHOUT the new needle: PASS on sh and on pwsh. So the new needle is the only thing that catches the drift; the code-family diff alone does not.\n- Unmutated `new` tree: PASS on sh, pwsh and PS 5.1.\nShared tree after the edit: parity-check.sh PASS (Git Bash); parity-check.ps1 PASS on pwsh and PS 5.1.",
      "startedAt": "2026-09-19T16:48:29.794Z",
      "completedAt": "2026-09-19T16:49:54.869Z"
    },
    {
      "stepId": "step-5",
      "description": "VERIFY, sequentially, with MOE_POSTFLIGHT_TIMEOUT_SEC=400. Launch the ps1 harness from PowerShell.\n1. The sha256-repo arm is green on sh, pwsh 7 and PS 5.1.\n2. Full harnesses: postflight.sh, and postflight.ps1 on pwsh and PS 5.1.\n3. Parity on sh, pwsh and PS 5.1.\n4. node --test scripts/tests/agent-runtime.test.mjs scripts/tests/taskless-handoff.test.mjs.\n5. cd packages/moe-daemon && npx tsc --noEmit && npm test: green, with no count change (the daemon change is a comment).\n6. For complete_task, the verification is the full postflight.sh command, with its PASS lines as outputTail.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "All long runs used SNAPSHOT trees, never the live one, because the Codex session keeps editing the shared wrappers and harnesses. MOE_POSTFLIGHT_TIMEOUT_SEC=400; ps1 harnesses launched from PowerShell; runs were sequential.\n1. Arms: sha256-repo and teardown-sha256 each PASS on sh, pwsh 7.6.6 and PS 5.1.26100 (step 2 note).\n2. Full harnesses:\n   - postflight.sh on D:/moe-scratch/w0747-1KZuRv/new (HEAD scripts + this patch): `PASS frozen candidate scenarios: 71` (69 + 2 new), `commit-scope scenarios run: 30`, `PASS postflight.sh`, rc=0, ~59 min.\n   - postflight.ps1 on `new` FAILED at the hot-reload arm: \"HOT RELOAD FAILED: no first claim (waited up to 400s; wrapper exited: True; RPC sequence '')\". PRE-EXISTING on HEAD and not from this task. HEAD 469ed6d carried the Codex hunk `& node $promptCacheHelper check ...; if ($LASTEXITCODE -ne 0) { exit 1 }` into moe-agent.ps1, and the arm copies only moe-agent.ps1 into a temp scripts/ with no helper beside it. Proof: pure HEAD scripts (no arms, no fix, helpers present) on pwsh give `PASS frozen candidate scenarios: 69` then the identical HOT RELOAD FAILED, rc=1.\n   - Clean base, to verify this change without the carry: tree `clean` = HEAD scripts, with moe-agent.ps1 from 021f351 (git diff 021f351 469ed6d touches only moe-agent.ps1, all prompt-cache hunks) + this patch, and no helpers. postflight.ps1 on pwsh 7.6.6: frozen 71, `[hot reload] ok: 2 reloads, 1 wrapper process, 1 runner identity`, scope 30, `PASS postflight.ps1`, rc=0, 609 s. On PS 5.1: the same PASS lines, rc=0, 562 s.\n3. Parity PASS on sh (Git Bash), pwsh and PS 5.1, on the shared tree and on the scratch trees (step 4).\n4. `node --test scripts/tests/agent-runtime.test.mjs scripts/tests/taskless-handoff.test.mjs`: 61/61 on the snapshot and 61/61 on the shared tree.\n5. packages/moe-daemon (shared tree): `npx tsc --noEmit` rc=0; `npm test` (vitest): 123 files, 2154/2154 passed. The count matches the prior shared-tree baseline (2154 = 2153 clean + the peer-dirty recommendSkill.test.ts it()). No count change is possible from this task: the daemon edit is a comment.\nExtra fail-open probes:\n- sh PATH-shim `git` that echoes `--show-object-format` back (git < 2.29) or exits 129 on it: 'pass' still lands (rc=0, so the set -e guard holds); 'sha256-repo' falls back to today's path. The shim log shows the probe was hit.\n- ps1: Get-MoeGitTop extracted by AST on pwsh and PS 5.1, called twice on a sha256 repo: $null both times, exactly 1 WARN, $LASTEXITCODE 0. A sha1 repo is unchanged. With an old-git git.cmd shim, the sha256 repo passes through (fail-open) with no WARN.\nHOLD: per the human (relayed by architect-74607b1b, msg-de6bc910), complete_task waits until the Codex prompt-cache work is committed on its own. The final verification is re-run against that new HEAD before completing.",
      "startedAt": "2026-09-19T16:50:04.663Z",
      "completedAt": "2026-09-19T18:24:38.547Z"
    },
    {
      "stepId": "step-6",
      "description": "SELF-REVIEW, posted as a task comment:\n1. Does any path touch git in a sha256 repository without going through the probe? List the git call sites you checked.\n2. Does a SHA-1 repository behave byte-for-byte as before? The full harnesses answer this.\n3. Is the refusal line printed once per process, not on every loop iteration?\n4. Does an old git without --show-object-format fall through to today's behaviour?\n5. Is the finalize outcome in a sha256 repository 'nothing-to-commit', and does the task's strict-policy consequence (no completion commit) match what the docs now say?\n6. git diff --stat: only the 9 planned paths.",
      "status": "COMPLETED",
      "affectedFiles": [],
      "note": "Self-review posted as comment-46c9ea8a. It answers all six questions. The only fix the review needed was already made in step 2: the sh teardown bypass, now routed through the probe and pinned by teardown-sha256. Theoretical notes are in the comment: echo -e on backslash paths, which matches the existing idiom; adoption wording in no-git and sha256 repos, which predates this task; and the hot-reload red caused by the 469ed6d carry. No files changed in this step. HOLD: complete_task waits for the Codex prompt-cache commit, per the human's option 1.",
      "startedAt": "2026-09-19T18:24:41.912Z",
      "completedAt": "2026-09-19T18:25:50.201Z"
    }
  ],
  "planRevision": 13,
  "status": "REVIEW",
  "assignedWorkerId": "qa-a4402b0a",
  "branch": null,
  "prLink": null,
  "reopenCount": 0,
  "reopenReason": "Human approved: SHA-256 object-format follow-up requested by QA of task-31fe6dc8.",
  "createdBy": "GOVERNOR",
  "parentTaskId": null,
  "dependsOn": [
    "task-31fe6dc806d9466d9d71c9f8beda60be",
    "task-d144136406384f96b85388da77f193ed",
    "task-84150869c3194fce908e6efad8f4c82b",
    "task-a5fb02e977f74ea2802f24053c44a1c6"
  ],
  "priority": "LOW",
  "order": 213.5,
  "comments": [
    {
      "id": "comment-46c9ea8a282945e4a3bd2f1297d81c35",
      "author": "worker-0966ef4c",
      "content": "SELF-REVIEW (step 6), worker-0966ef4c\n\n1. Git in a sha256 repo without the probe? No.\n- sh: git_top callers are preflight_landing, receipt_replay (locals), the post-flight landing and no-landing branches, and adoption. Every other git call is keyed on MOE_TOP/MOE_GITDIR (set only by git_top), on the hook $scratch or on $GATE_WORKSPACE, both inside a landing.\n- The ONE bypass was teardown_rescue's direct `rev-parse --show-toplevel`. It touched no ref, but gave an interrupted sh session 'failed' instead of 'nothing-to-commit'. It now runs `[ -n \"${MOE_TOP:-}\" ] || (git_top) || return 0`; the subshell keeps MOE_TOP as the session left it, so SHA-1 outcomes are unchanged. Pinned by the new teardown-sha256 arm.\n- ps1: Get-MoeGitTop is called by receipt replay, idle recovery, teardown, pre-flight, the three post-flight branches and adoption. Every other git call takes its Top from it, or from the hook scratch / gate workspace.\n2. SHA-1 byte-for-byte? The only new work per probe is one `rev-parse --show-object-format`. Snapshot HEAD+patch: full postflight.sh PASS (frozen 71 = 69 old + 2 new, scope 30). Clean base (021f351 ps1 + patch): full postflight.ps1 PASS on pwsh and PS 5.1 (frozen 71, hot reload ok, scope 30). Daemon 2154/2154, node 61/61, parity PASS on all three shells.\n3. Once per process? Yes. The flag is a global in sh and $script: in ps1. The sha256-repo arm asserts exactly 1 WARN in a run that calls the probe at least 3 times. Calling ps1 Get-MoeGitTop (AST-extracted) twice on a sha256 repo gives $null both times and 1 WARN, on both engines. A hot reload (sh exec, ps1 new script scope) prints it once more, by design, and the comments say so.\n4. Old git? rev-parse ECHOES an unknown flag with rc 0 (measured), so a pre-2.29 git answers '--show-object-format' and proceeds. PATH shims that echo it back or exit 129: sh 'pass' still lands (the set -e guard holds) and sha256-repo falls back to today's path; ps1 AST probe with a git.cmd shim fails open, with no WARN.\n5. Outcome is 'nothing-to-commit' on both twins, including teardown (arm-pinned). Strict policy: no completion commit is ever recorded, so local-branch and remote-push cannot be met. CONFIGURATION.md autoCommit now says to follow the no-git rules (manual-artifact, with qualityGate unset or autoCommit false), consistent with the deliveryPolicy row.\n6. Diff: my hunks touch exactly the 9 planned paths. moe-agent.sh 19/3, moe-agent.ps1 13/0, postflight.sh 22/6, postflight.ps1 22/6, parity-check.sh 1/1, parity-check.ps1 1/1, candidateStore.ts 4/0, CONFIGURATION.md 1/1, SCHEMA.md 1/1. The shared moe-agent.{sh,ps1} and postflight.{sh,ps1} ALSO hold the Codex session's uncommitted prompt-cache hunks, which are not mine. Per the human (architect msg-de6bc910), I hold complete_task until that work is committed on its own. My hunks alone are in D:/moe-scratch/w0747-1KZuRv/mine.patch; `git apply --cached -R` of it was verified in a throwaway index.\n\nTheoretical, not fixed:\n- sh `echo -e` would interpret a backslash in the repo path. Every other sh WARN in the wrapper uses the same idiom.\n- In a sha256 (or no-git) repo, the adoption path still says \"the tree is clean - nothing to land\". That wording predates this task; the WARN line precedes it.\n- A pre-existing red, not mine: HEAD's own postflight.ps1 hot-reload arm has failed since 469ed6d carried the prompt-cache check without its helper.",
      "timestamp": "2026-09-19T18:25:45.676Z"
    }
  ],
  "hasPendingQuestion": false,
  "createdAt": "2026-09-18T22:04:32.874Z",
  "updatedAt": "2026-09-20T11:06:06.120Z",
  "metrics": {
    "firstClaimAt": "2026-09-19T08:50:55.263Z",
    "plannedStepCount": 6,
    "plannedDistinctFileCount": 9,
    "executedStepCount": 6
  },
  "contextFetchedBy": [
    "architect-e52c9adc",
    "worker-0966ef4c",
    "qa-a4402b0a"
  ],
  "planSizeWarnings": [
    "Plan touches 9 distinct files (target <=5); plans over 10 are rejected. Small file-disjoint tasks review better and parallelize."
  ],
  "stepsCompleted": [
    "step-1",
    "step-2",
    "step-3",
    "step-4",
    "step-5",
    "step-6"
  ],
  "failedDodItems": [],
  "planSubmittedAt": "2026-09-19T08:53:01.330Z",
  "planningNotes": {
    "approachesConsidered": "- Accept 64-hex ids end to end: rejected for this slice. It needs daemon regexes and messages widened, every '7-40 hex' and '40 hex' mention in the tool descriptions and docs updated, the wrappers' zero oid (40 zeros) and empty-tree sentinel (the SHA-1 4b825dc6…) made format-aware, the replay journal regexes widened in both twins, and the harness fakes widened. That is broad risk for a rare format, against the 'one narrow vertical slice' rail.\n- Force autoCommit=false in the settings reader: rejected. The settings readers don't know the repository yet. git_top and Get-MoeGitTop are the single entry every git path already goes through, and their 'no repo' result is the existing, tested manual/no-git path.\n- Also improving the daemon's 64-hex refusal messages: YAGNI. The wrappers are the only runners in this slice, and after this change they never send a 64-hex id.",
    "codebaseInsights": "- The single git probe is sh git_top() (7 call sites) and ps1 Get-MoeGitTop (9 call sites). Returning 'no repo' there sends every git path down the no-git branch; the frozen suite's 'no-git' mode already pins that branch's finalize outcome, nothing-to-commit.\n- After task-31fe6dc8, SHA_RE (7-40) and REVISION_RE (40) live once in candidateStore.ts.\n- The frozen-suite fixture is created with git(repo,'init','-q'); the new mode swaps in --object-format=sha256.\n- The fake proxy's candidateReply checks 40-hex shas like the daemon, which is what makes the old-code run go red, with a rescue.\n- git rev-parse --show-object-format exists since git 2.29, the same release that added sha256 repositories.",
    "risks": "- dependsOn task-31fe6dc8 (DONE), task-d1441364 and task-84150869 (the wrappers and harnesses) and task-a5fb02e9 (candidateStore.ts comments), all of which edit these files. Start after them.\n- The probe adds one git call per git_top: cheap, but git_top runs on every landing. Keep it to a single rev-parse.\n- git_top is sometimes called in a subshell (sh), where the print-once flag does not persist; make sure the line is not spammed in a loop.\n- Under a strict delivery policy, a sha256 project cannot reach DONE without an attestation, which is the correct and documented consequence.",
    "keyFiles": [
      "scripts/moe-agent.sh",
      "scripts/moe-agent.ps1",
      "scripts/tests/postflight.sh",
      "scripts/tests/postflight.ps1",
      "packages/moe-daemon/src/state/candidateStore.ts",
      "docs/CONFIGURATION.md",
      "docs/SCHEMA.md"
    ]
  },
  "planApprovedAt": "2026-09-19T08:53:01.406Z",
  "workStartedAt": "2026-09-19T16:32:23.814Z"
}